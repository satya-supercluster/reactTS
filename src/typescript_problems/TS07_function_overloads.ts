        /**
         * Problem 7: Function Overloads
         *
         * Description:
         * Create an overloaded function parseInput that:
- Accepts a string and returns string[] (split by commas)
- Accepts a string[] and returns a joined string (comma-separated)
Use function overload signatures properly.
         *
         * Sample Input:
         * parseInput('a,b,c'), parseInput(['a','b','c'])
         *
         * Sample Output:
         * ['a','b','c'], 'a,b,c'
         */
