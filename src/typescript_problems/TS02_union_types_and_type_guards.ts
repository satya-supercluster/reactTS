        /**
         * Problem 2: Union Types and Type Guards
         *
         * Description:
         * Create a function formatId(id) that accepts either a number or a string.
- If id is a number, return 'ID-<number>'
- If id is a string, return 'ID-<string>' but uppercase.
Use typeof checks (type guards) to handle both branches safely.
         *
         * Sample Input:
         * formatId(42), formatId('abc')
         *
         * Sample Output:
         * 'ID-42', 'ID-ABC'
         */
