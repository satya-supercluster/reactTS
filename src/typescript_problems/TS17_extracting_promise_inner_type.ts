        /**
         * Problem 17: Extracting Promise Inner Type
         *
         * Description:
         * Create a conditional type UnwrapPromise<T> that extracts the inner type of a Promise.
Example: UnwrapPromise<Promise<number>> should be number.
Demonstrate with multiple examples.
         *
         * Sample Input:
         * type A = UnwrapPromise<Promise<string>>
         *
         * Sample Output:
         * A is string (as a type).
         */
