        /**
         * Problem 11: Generic Identity Function
         *
         * Description:
         * Implement a generic function identity<T>(value: T): T.
Demonstrate using it with number, string, and a custom interface.
Show how type inference works without explicitly passing <T>.
         *
         * Sample Input:
         * identity(42), identity('hello'), identity({ id:1 })
         *
         * Sample Output:
         * Returns same value with correct inferred type in each call.
         */
